# App Store Connect configuration

app_identifier("dev.midorinome.ichime")
apple_id(ENV["APPLE_ID"]) # Optional: Add APPLE_ID to secrets if needed
team_id("YBBAN7R28K")

# For multiple targets
for_platform :ios do
  for_lane :testflight do
    app_identifier("dev.midorinome.ichime")
  end
end
